[package]
name = "chimera_core"
version = "2.0.0"
edition = "2021"
description = "Sovereign mesh core for ruby_llm_mesh — native node + intent execution"
license = "MIT"

[lib]
name = "chimera_core"
crate-type = ["cdylib"]

[profile.release]
lto = true
opt-level = 3
